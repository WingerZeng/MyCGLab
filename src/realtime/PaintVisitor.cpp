#include "PaintVisitor.h"

namespace mcl{
	
	int PaintVisitor::paintTris(PaintInfomation* info, PTriMesh* tri)
	{
		LOG(FATAL) << "This method is unimplemented!";
		return 0;
	}

	int PaintVisitor::initTris(PTriMesh* tri)
	{
		LOG(FATAL) << "This method is unimplemented!";
		return 0;
	}

	int PaintVisitor::paintPoint(PaintInfomation* info, PPoint* point)
	{
		LOG(FATAL) << "This method is unimplemented!";
		return 0;
	}

	int PaintVisitor::initPoint(PPoint* point)
	{
		LOG(FATAL) << "This method is unimplemented!";
		return 0;
	}

	int PaintVisitor::paintPolygons(PaintInfomation* info, PPolygonMesh* polygon)
	{
		LOG(FATAL) << "This method is unimplemented!";
		return 0;
	}

	int PaintVisitor::initPolygons(PPolygonMesh* polygon)
	{
		LOG(FATAL) << "This method is unimplemented!";
		return 0;
	}

	int PaintVisitor::paintLines(PaintInfomation* info, PLines* lines)
	{
		LOG(FATAL) << "This method is unimplemented!";
		return 0;
	}

	int PaintVisitor::initLines(PLines* lines)
	{
		LOG(FATAL) << "This method is unimplemented!";
		return 0;
	}

}